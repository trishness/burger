<li>
	{{burger_name}}

	<button type="button" class="btn btn-secondary btn-sm change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour It!{{else}}I'd like another!{{/if}}
	</button>


</li>
